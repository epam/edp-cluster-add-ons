{{- if and (.Values.harborHA) (.Values.harborHA.enable) -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: harbor-ha
  namespace: {{ .Values.argoNamespace | default "argocd" }}
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.argoProject | default "default" }}
  source:
    repoURL: {{ .Values.repoUrl }}
    path: add-ons/harbor-ha
    targetRevision: {{ .Values.targetRevision }}
    helm:
      values: |
        harbor:
        {{- toYaml .Values.harborHA | nindent 10 }}
  destination:
    server: {{ .Values.destinationServer | default "https://kubernetes.default.svc" }}
    namespace: harbor
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace={{ .Values.harborHA.createNamespace }}
    retry:
      limit: 1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
{{- end -}}
