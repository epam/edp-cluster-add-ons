{{- if and (.Values.vaultOKD) (.Values.vaultOKD.enable) -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-okd
  namespace: {{ .Values.argoNamespace | default "argocd" }}
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.argoProject | default "default" }}
  source:
    repoURL: {{ .Values.repoUrl }}
    path: add-ons/vault-okd
    targetRevision: {{ .Values.targetRevision }}
    helm:
      values: |
        vault:
        {{- toYaml .Values.vaultOKD | nindent 10 }}
  destination:
    server: {{ .Values.destinationServer | default "https://kubernetes.default.svc" }}
    namespace: vault
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace={{ .Values.vaultOKD.createNamespace }}
    retry:
      limit: 1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
{{- end -}}
