apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "nexus-ce.name" . }}-properties
data:
  nexus.properties: |
    nexus.scripts.allowCreation=true
    jetty.httpConfig.requestHeaderSize=100000
